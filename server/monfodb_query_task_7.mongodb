// task 7
// Необхідно знайти і порахувати, скільки записів у колекції
// Messages містять слово "паровоз" . Використовувати агрегацію
// для цих цілей. Підрахунок виконувати на стороні БД.
// Усе це - за 1 запит.


//use shm-chat;

db.messages.aggregate([
   {
      $match: { body: /паровоз/ }
   },
   
      {
      $group: { _id: null, count: { $count: {} } }
   }

]);